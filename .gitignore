build/
build-*/
*.gz
*.NASDAQ_ITCH50
*.itch
.DS_Store
.cache/
compile_commands.json
__pycache__/

# lcov tracefiles regenerated by coverage/README.md's local recipe (and by
# CI's `coverage` job) -- multi-megabyte, machine-specific absolute paths
# baked in by geninfo. coverage/coverage.json (the small badge summary) is
# the only file under coverage/ that's meant to be committed.
coverage/lcov.info
coverage/lcov.filtered.info
