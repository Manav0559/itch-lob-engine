## Checklist

- [ ] Builds clean with `-Werror` (no new warnings)
- [ ] `ctest --test-dir build --output-on-failure` passes
- [ ] New/changed behavior has a test
- [ ] Docs updated if behavior or architecture changed (`README.md`, `docs/architecture.md`)
